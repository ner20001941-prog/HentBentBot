services:
  - type: web
    name: telegram-bot
    env: python
    pythonVersion: "3.9.0"
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    healthCheckPath: /health
